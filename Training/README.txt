This folder include 4 training codes.

Dependencies have been exported to requirements.txt after training, which could be found along with the training code. 

Command used:
!pip freeze > requirements.txt

SEEDS are used in the code for reproducibility.